{
  "name": "savelycloud",
  "version": "1.0.0",
  "private": true,
  "description": "SavelyCLOUD, a dependency-free private cloud storage server for your local network.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "pair-upload": "node tools/pair-upload.js"
  },
  "engines": {
    "node": ">=20"
  }
}
